Cloud
download Git bash

//git-commands

git config --gloabal user.name "github username"

git config --gloabal user.email "github email"

git config --global --list

git init (to create local repository)

git add <filename> (to add file to send github)

git commit -m "tag" (to use add tag to the file)

git branch -M

git remote add origin <github repo http link>

git push -u origin <branch name>